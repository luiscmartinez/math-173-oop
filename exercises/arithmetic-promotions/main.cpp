#include <iostream>

int main()
{
    // arithmetic (promotions):
    std::cout << 'x' + 2;
    // one liner for many insertions to different vars
    std::cout << "enter 3 ints:";
    int nums[3];
    std::cin >> nums[0] >> nums[1] >> nums[2];
}